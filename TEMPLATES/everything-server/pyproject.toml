[project]
name = "everything-server"
version = "0.1.0"
description = "Sample MCP server exposing echo, now, add, and reverse tools."
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
  "fastmcp>=2.0.0",
]

[project.scripts]
everything-server = "server:mcp.run"
